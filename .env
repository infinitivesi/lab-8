# Environment variables for docker-compose (copy to .env and adjust as needed)
HOST=0.0.0.0
PORT=5000
# Path inside container where SQLite DB is stored
DB_PATH=/data/db.sqlite
# Optionally pin image tag or other vars
# DEBUG=0
